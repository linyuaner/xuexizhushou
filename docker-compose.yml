# =============================================================
# docker-compose.yml - 学习助手一键部署
# =============================================================

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      # 利用 BuildKit 缓存加速重复构建
      cache_from:
        - linyuaner/xuexizhushou:latest
    image: linyuaner/xuexizhushou:latest
    container_name: study-helper-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # JWT 密钥（生产环境必须设置）
      - JWT_SECRET=${JWT_SECRET:-}
      # 热更新配置
      - ENABLE_HOT_UPDATE=true
      - HOT_UPDATE_INTERVAL=300
      - GITHUB_REPO=linyuaner/xuexizhushou
    volumes:
      # 数据持久化：SQLite 数据库文件
      - quiz-data:/app/server/data
      # 文件上传持久化
      - quiz-uploads:/app/server/uploads
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      start_period: 30s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  quiz-data:
    driver: local
  quiz-uploads:
    driver: local
